// Generated by CoffeeScript 1.6.2
(function() {
  var Fantomo, bot, debug;

  process.stdout.write('\u001B[2J\u001B[0;0f');

  debug = require('debug')('fantomo:examples:bot-debug');

  Fantomo = require('..');

  bot = new Fantomo({
    verbose: true,
    browser: {
      verbose: true
    }
  });

  bot.on('ready', function() {
    debug('bot is ready');
    return this.open('http://moul.github.io/');
  });

  bot.browser.on('ready', function() {
    return debug('browser is ready');
  });

  bot.browser.on('open', function(path) {
    return debug("browser is open (" + path + ")");
  });

}).call(this);
